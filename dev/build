#!/bin/bash
set -e

# make sure script works when called from any directory
cd "$(dirname "$(readlink -f "$0")")"

echo building image...

podman build \
  --file Containerfile \
  --format docker \
  --tag solar-protocol ..
