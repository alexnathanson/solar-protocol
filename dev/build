#!/bin/bash
set -e

# make sure script works when called from any directory
cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null

echo building image...

podman build \
  --file Containerfile \
  --format docker \
  --tag solar-protocol ..
