# envsubst < config.template > config

PI_GEN_RELEASE="Solar Protocol $VERSION"
TARGET_HOSTNAME="solar-protocol"
KEYBOARD_KEYMAP="us"
TIMEZONE_DEFAULT="America/New_York"
KEYBOARD_LAYOUT="English (US)"
FIRST_USER_NAME="pi"
FIRST_USER_PASS="$FIRST_USER_PASS"
WPA_COUNTRY="US"
ENABLE_SSH="1"
DEPLOY_COMPRESSION="xz"
PUBKEY_ONLY_SSH="1"
PUBKEY_SSH_FIRST_USER="$PUBKEY_SSH_FIRST_USER"
STAGE_LIST="stage0 stage1 stage2 stage-solar"
IMG_NAME="solar-protocol"
